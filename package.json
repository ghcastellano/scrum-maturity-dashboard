{
  "name": "scrum-maturity-dashboard",
  "version": "1.0.0",
  "description": "Scrum Maturity Model Dashboard with Jira Cloud Integration",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build:all": "cd server && npm install && cd ../client && npm install && npm run build && cd .. && mkdir -p server/public && cp -r client/dist/* server/public/",
    "start": "cd server && npm start"
  },
  "keywords": ["scrum", "jira", "metrics", "maturity-model", "agile"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
